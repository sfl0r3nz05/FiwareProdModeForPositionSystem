# Deploy Quantum Leap component

A Helm chart for running the fiware orion context broker on kubernetes.

1. Installation

    ```console
    cd ~/FiwareProdModeForPositionSystem/quantumleap
    ```

    ```console
    helm install quantumleap . -n fiware
    ```
