apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-claim0
  labels:
    app: nginx-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
